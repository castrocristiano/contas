[project]
name = "contas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cristiano", email = "cristiano@local" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
contas = "contas:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
